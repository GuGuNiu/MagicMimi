<Window x:Class="MagicMimi.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MagicMimi"
        mc:Ignorable="d"
        Title="咕咕牛万能咪咪" Height="720" Width="1100"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <SolidColorBrush x:Key="MagicBorderLightBrush" Color="#bbf7d0"/>
        <SolidColorBrush x:Key="MagicShadowBrush" Color="#10b981"/>
        <SolidColorBrush x:Key="MagicTextMediumBrush" Color="#059669"/>
        <SolidColorBrush x:Key="MagicBgBrush" Color="#f0fdfa"/>

        <Style x:Key="MimiButtonBase" TargetType="Button">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="10" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="2" Direction="315" Color="{Binding Color, Source={StaticResource MagicShadowBrush}}" BlurRadius="0" Opacity="1"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" TextBlock.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.95"/>
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform X="-1" Y="-1"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="1"/>
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform X="0" Y="0"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button" BasedOn="{StaticResource MimiButtonBase}">
            <Setter Property="Height" Value="50"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Background" Value="#a7f3d0"/>
            <Setter Property="Foreground" Value="#047857"/>
            <Setter Property="BorderBrush" Value="#34d399"/>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource MimiButtonBase}">
            <Setter Property="Height" Value="50"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Background" Value="#fee2e2"/>
            <Setter Property="Foreground" Value="#991b1b"/>
            <Setter Property="BorderBrush" Value="#fca5a5"/>
        </Style>

        <Style x:Key="ToggleScanButtonStyle" TargetType="Button">
            <Setter Property="Style" Value="{StaticResource ActionButton}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsScanning}" Value="True">
                    <Setter Property="Style" Value="{StaticResource DangerButton}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="WindowControlButton" TargetType="Button" BasedOn="{StaticResource MimiButtonBase}">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Background" Value="#f1f5f9"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="BorderBrush" Value="#94a3b8"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Border x:Name="MainBorder" CornerRadius="10" BorderThickness="1" BorderBrush="{StaticResource MagicBorderLightBrush}" Background="{StaticResource MagicBgBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" Background="{StaticResource MagicBgBrush}" Margin="10,0,10,0">
                    <Button x:Name="MinButton" Style="{StaticResource WindowControlButton}" Content="-" Click="MinButton_Click"/>
                    <Button x:Name="CloseButton" Style="{StaticResource WindowControlButton}" Content="x" Click="CloseButton_Click"/>
                </StackPanel>

                <Border Grid.Row="1" CornerRadius="0,0,10,10" Background="{StaticResource MagicBgBrush}" BorderBrush="{StaticResource MagicBorderLightBrush}" BorderThickness="1">
                    <!-- Main content goes here -->
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>