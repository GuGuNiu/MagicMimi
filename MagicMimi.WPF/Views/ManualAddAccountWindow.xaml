<Window x:Class="MagicMimi.WPF.Views.ManualAddAccountWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="手动添加账户" Height="300" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Border CornerRadius="16" BorderThickness="2" BorderBrush="{StaticResource MagicBorderLightBrush}" Background="{StaticResource MagicPanelBgBrush}">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="4" Direction="270" Color="{Binding Source={StaticResource MagicShadowBrush}, Path=Color}" Opacity="0.8" BlurRadius="4" />
            </Border.Effect>
            <StackPanel Margin="25">
                <TextBlock Text="手动添加 Stoken 账户" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="{StaticResource MagicTextDarkBrush}" Margin="0,0,0,15"/>

                <TextBlock Text="账户名称:" Foreground="{StaticResource MagicTextDarkBrush}" Margin="0,0,0,5"/>
                <TextBox x:Name="AccountNameTextBox" Style="{StaticResource MimiTextBoxStyle}" Margin="0,0,0,10"/>

                <TextBlock Text="Stoken Cookie:" Foreground="{StaticResource MagicTextDarkBrush}" Margin="0,0,0,5"/>
                <TextBox x:Name="StokenCookieTextBox" Style="{StaticResource MimiTextBoxStyle}" Margin="0,0,0,15" TextWrapping="Wrap" AcceptsReturn="True" Height="60" VerticalContentAlignment="Top"/>

                <Button Content="保存账户" Click="SaveButton_Click" Style="{StaticResource MimiButtonActionStyle}" HorizontalAlignment="Stretch"/>
            </StackPanel>
        </Border>
        <Button Content="✕" Style="{StaticResource WindowControlButton}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,10,0" Click="CloseButton_Click"/>
    </Grid>
</Window>